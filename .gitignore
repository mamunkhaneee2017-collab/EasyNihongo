node_modules/
.env
.DS_Store
Thumbs.db
*.log
database/*.sqlite
database/sessions/
uploads/
.claude/settings.local.json

# Vendored stroke-order source data (build-time only — see scripts/README.md).
# The generator scripts read from here once; their output is committed instead.
vendor/
